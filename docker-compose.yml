version: '3.9'

services: 
    nginx:
        build: 
            dockerfile: Dockerfile
            context: ./nginx
        container_name: nginx
        image: danilocostaolist/nginx
        ports: 
            - "8080:80"
        working_dir: /var/www/html
        volumes:
            - ./:/var/www/html
